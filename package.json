{
  "name": "node_weather_console_app",
  "version": "1.0.0",
  "description": "console app which allows user to specify command line arguments for the location and fetches a weather summary from the open weather api before printing it to the console.",
  "main": "weather.js",
  "scripts": {
    "start": "nodemon app.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Nick Callaghan",
  "license": "MIT",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^8.2.0",
    "express": "^4.18.2",
    "node-fetch": "^2.6.0",
    "nodemon": "^2.0.1"
  }
}
